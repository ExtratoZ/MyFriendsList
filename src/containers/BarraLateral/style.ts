import styled from 'styled-components';
import variaveis from '../../styles/variaveis';

export const Aside = styled.aside`
  padding: 16px;
  height: 100vh;
  border-right: 1px solid ${variaveis.secondary};
`;
